using UnityEngine;
using System.Collections;

public class LoginForm : MonoBehaviour
{

    public GameObject edUsername;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

    void OnClick()
    {
        //VariableApplication.idUserLogined = edUsername.GetComponentInChildren<UILabel>().text;
        //LamaControllib.getInstance().getUserModel().IdUser = VariableApplication.idUserLogined;
        //LamaControllib.getInstance().getUserModel().Username = "Tính Đẹp Trai " + VariableApplication.idUserLogined;

        //Application.LoadLevel("OptionScreen");
    }
}
